# Estado persistente del proyecto

> Este archivo es un resumen operativo, no un historial completo. Mantenlo corto y actualizado.

## Objetivo actual

Construir y estabilizar la aplicacion web de propiedad horizontal en este repositorio.

## Estado

- Proyecto activo: `PROPIETY-HORIZON`.
- Frontend: revisar `src/`, `vite.config.ts` y `package.json` antes de editar.
- Backend: revisar `server/` y `backend/` cuando la tarea toque API o datos.
- El modelo local se ejecuta con Ollama y OpenCode usa `ollama/qwen-claude-backend`.

## Protocolo de continuidad

- La memoria activa del modelo es limitada a 32k; la continuidad de la jornada vive en este archivo y en el codigo.
- Mantener este archivo por debajo de unas 1.500 palabras.
- En cada hito registrar objetivo actual, decisiones, archivos tocados, pruebas, bloqueos y proxima accion.
- No guardar aqui conversaciones completas, logs largos ni codigo duplicado.

## Convenciones de trabajo

- Investigar con busqueda selectiva y editar por cambios pequenos.
- Verificar con los scripts definidos en `package.json`.
- Registrar aqui decisiones permanentes, archivos importantes y bloqueos.

## Proxima accion

Leer el estado real del repositorio y continuar con la tarea que indique el usuario; no asumir que esta nota sustituye al codigo.
